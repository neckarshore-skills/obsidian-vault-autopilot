---
tags:
  - ai-ml
  - ai_ml
  - ai/ml
---

`ai-ml` and `ai_ml` are separator variants (a functional-duplicate group).
`ai/ml` is a NESTED tag — semantically distinct, must NOT be grouped with them.
