# Internal Changelog

Per-component implementation log for the obsidian-vault plugin. All skills and references log their changes here in chronological order, with full technical detail.

> **For user-facing release notes**, see the top-level [`CHANGELOG.md`](../CHANGELOG.md). This file is the internal companion — it documents what was changed at the file / function level, including findings codes and Gold-Run cell references that are not user-visible.

| Date | Component | Change |
|------|-----------|--------|
| 2026-04-01 | inbox-sort | Initial Claude Code version — vault-agnostic, dynamic subfolder discovery |
| 2026-04-01 | references/web-capture-detection | Created shared detection rules (extracted from previous inbox-sort + note-rename) |
| 2026-04-01 | logs | Created shared changelog and run history |
| 2026-04-01 | note-rename | Initial Claude Code version — content-based renaming, backlink updates |
| 2026-04-01 | note-quality-check | Initial Claude Code version — scoring, deletion recommendations |
| 2026-04-01 | property-describe | Initial — AI-generated description summaries (max 250 chars) |
| 2026-04-01 | property-classify | Initial — status (lifecycle) + type (category) in one pass |
| 2026-04-01 | property-enrich | Initial — fill missing metadata (title, dates, aliases, parent, source, priority) |
| 2026-04-01 | tag-manage | Initial — audit + suggest tags (consolidated from 3 previous skills) |
| 2026-04-01 | references/tag-convention | Created shared tag convention reference |
| 2026-04-01 | **Phase 2 complete** | 7/10 skills done. Phase 3 (social-post, social-scrape, research-report) deferred |
| 2026-04-27 | **v0.1.1** | Pre-launch hardening release (Windows safety + plugin updatability) |
| 2026-04-27 | references/windows-preflight | Non-skippable wording — explicit "run on EVERY invocation, no caching across turns" directive at the top, plus parallel wording in each launch-scope SKILL.md Pre-flight section. Closes Resume-Session-Skip risk identified in 2026-04-27 b live-validation. |
| 2026-04-27 | references/windows-preflight | Recovery command shortened from PowerShell `New-ItemProperty` (~130 chars) to `reg add` from elevated cmd.exe (~95 chars) — reduces paste-truncation risk on Windows clipboards. Verify command also switched to `reg query` for consistency. |
| 2026-04-27 | docs/windows-considerations | Added "Session Discipline & First-Run Gotchas" section — 5 platform behaviors first-time Windows users should know: Resume-Session-Skip, PowerShell first-use authorization, project-vs-user plugin enablement, OAuth right-click paste, why marketplace updates may not update. |
| 2026-04-27 | plugin.json + marketplace.json | Version bump 0.1.0 → 0.1.1. Marketplace caches by version field; existing 0.1.0 installs need this bump to receive updates. |
| 2026-04-27 | property-describe + README + ROADMAP | property-describe Pre-flight section added (4-of-4 launch-scope skills now gated). README + ROADMAP launch-scope language reconciled to 4-skill scope (matches Gary 2026-04-23b marketing posts on neckarshore-ai-hq, commit `7f5bc98`). Closes Drift between marketing-source-of-truth (4 skills) and code-source-of-truth (was 3 skills). |
| 2026-04-27 | **v0.1.2** | YAML-edit hardening + findings convention |
| 2026-04-27 | references/yaml-edits | Created — canonical line-by-line spec for YAML/Markdown edits. Forbids multi-line regex (`(?s)`, `(?m)`, `.+`/`.*` over newlines) and `str.replace` for non-atomic edits. Includes recipes for read-frontmatter / replace-field / add-field / append-to-list / append-callout-row, plus DO-NOT examples for F8 (callout `> ` blockquote prefix) and F15 (`tags:` greedy regex). Closes mid-run regex bugs identified in F8/inbox-sort GR-1 (93/105 missing move-row) and F15/property-enrich GR-2 (16 orphan `- VaultAutopilot` lines). |
| 2026-04-27 | references/findings-file | Created — vault-side findings convention. Path `<VAULT>/_vault-autopilot/findings/<YYYY-MM-DD>-<skill>.md`. Append-only ledger, never edit prior findings. Obi reads on session start via `ls _vault-autopilot/findings/*.md`. |
| 2026-04-27 | references/vault-autopilot-note | Added Protected Files rule 7: `_vault-autopilot/` folder is plugin-managed, written only during the findings-write step. |
| 2026-04-27 | inbox-sort + note-rename + property-enrich + property-describe | Added cross-references to `references/yaml-edits.md` in YAML-touching workflow steps; added "Write findings file" step before final report. property-enrich workflow Step 5 wording tightened from "add fields, preserve values" to explicit line-by-line mandate. property-enrich Quality Check extended with line-by-line + findings-file items. |
| 2026-04-27 | plugin.json + marketplace.json | Version bump 0.1.1 → 0.1.2. Marketplace caches by version field; existing 0.1.1 installs need this bump to receive updates. |
| 2026-04-29 | **v0.1.3** | Quoted-key cluster repair + pre-write YAML sanity check (defense-in-depth) |
| 2026-04-29 | references/yaml-sanity.md | Created — pre-write defense-in-depth check called step-zero by every property-writing skill. Returns one of seven verdicts (`OK`, `OK_QUOTED`, `OK_NO_FRONTMATTER`, `BROKEN_KEYS_INSIDE_COLON`, `MULTIPLE_FRONTMATTER_BLOCKS`, `UNCLOSED_FRONTMATTER`, `INVALID_YAML`). Per-skill policy table maps verdict → action (proceed / repair / skip-with-finding). Detection-precedence rule distinguishes shape α (standard quoted-key, valid YAML) from shape β (inside-colon, broken-as-author-intended). |
| 2026-04-29 | references/yaml-edits.md | Recipe (f) addendum — Normalize inside-colon quoted-keys (F26 repair). Per-line regex `^(\s*)"([^"]+):"\s*:(.*)$`. Strips outside-colon AND inside-colon, preserves indent + value. Duplicate-key resolution: keep first, log Class-D. Idempotent. Worked example + DO-NOT block (multi-line regex over joined frontmatter). |
| 2026-04-29 | references/german-date-normalization.md | Created — DACH-locale `DD.MM.YYYY[, HH:mm:ss]` → ISO 8601 normalization for property-enrich Source Hierarchy Prio 1. Read-only normalization (additive-only contract preserved — YAML value not rewritten). |
| 2026-04-29 | property-enrich | Step 2a NEW (sanity-check + recipe f, broadened from v0.1.0/v0.1.2 hardcoded `"created:"`/`"modified:"` list). Pre-Write defense-in-depth (Step 5 calls sanity-check again, idempotent). Source Hierarchy Prio 1 extended with German-date normalization (F2). `scope` parameter unified (F18 — `inbox` default, `inbox-tree` opt-in for bulk-mode). 4 new Quality Checks. |
| 2026-04-29 | property-describe | Step 2a NEW (pre-flight sanity-check). Filter regex broadened (F25 — accepts plain `description:` AND standard quoted-key `"description":` shape α). Inside-colon shape β files SKIPPED with Class-C finding (additive-only boundary — describe does NOT repair, user runs property-enrich first). Pre-Write sanity-check defense-in-depth (Step 5). F7 source-of-content policy added (every claim traceable to body / URL-text / title; no fabrication). `scope` parameter unified (F18). 4 new Quality Checks. |
| 2026-04-29 | note-rename | Step 4a generalized — sanity-check + recipe (f), broadened from hardcoded `"created:"`/`"modified:"` to ALL quoted-key patterns. `scope` parameter unified (F18). 2 new Quality Checks. |
| 2026-04-29 | inbox-sort | Step 5a generalized — sanity-check + recipe (f), broadened from hardcoded list. 2 new Quality Checks. |
| 2026-04-29 | test-data | New fixtures: `f19-repro.md` (shape β `"created:"`/`"modified:"`, F19-LIVE), `f25-repro.md` (shape α `"description":`, F25 filter-regex), `f26-repro.md` (shape β cluster + duplicate-collision), `f26-mixed-shapes-repro.md` (shape α + β coexist), `f2-repro.md` (German DACH date format). Plus `expected/<skill>/*.md` golden files for diff-validation. |
| 2026-04-29 | scripts/smoke-test.sh | Created — manual harness for v0.1.x fixtures. Sets up temp vault, walks skills in declared order, diffs vs golden files. Timestamps normalized via `{{TIMESTAMP}}` placeholder + sed. Exit 0 on PASS, 1 on diff, 2 on missing fixture. v0.2.0+ may automate via Claude Agent SDK. |
| 2026-04-29 | plugin.json + marketplace.json | Version bump 0.1.2 → 0.1.3. Marketplace caches by version field; existing 0.1.2 installs need this bump to receive updates. |
| 2026-05-04 | **v0.1.4** | Public-Flip blocker fixes + mode-shift unification (W1 trailing-dot enumeration + W2 clone-cluster gate + W3 robocopy preflight + W4 recipe-f duplicate-key policy) |
| 2026-05-06 | references/windows-preflight.md | **v0.1.4 W1** — added Step 5 "Trailing-Dot Folder Detection" (Windows-only WARN, not STOP) and Step 6 "Windows-aware Enumeration Pattern" (per-language enumeration recipes for PowerShell `[System.IO.Directory]::EnumerateFiles` with `\\?\` extended-path prefix, Python `os.scandir` over `\\?\`-prefixed absolute path, Bash `find` pass-through on macOS/Linux, plus a cross-platform decision shorthand keyed on `IS_WINDOWS`). Closes F-NEW-A-1 (GR-3 nexus-clone-robocopy 2026-05-01: 670 files / 301 markdown silently invisible inside `030_Systems - reference material.` because Win32 path normalization independently strips trailing `.` and trailing space from path components, regardless of `LongPathsEnabled = 1`). 4 launch-scope SKILL.md files updated their parenthetical Pre-flight summary to reference the new Step 5 + Step 6 enumeration pattern. |
| 2026-05-06 | scripts/test-windows-trailing-dot.sh + tests/fixtures/windows-trailing-dot/ | **v0.1.4 W1** — new assertion harness + 5-file regression fixture. Fixture contains a folder named `030_Systems - reference material.` with two notes inside (one outgoing wikilink to a file outside, one bare note) plus two notes outside (one incoming wikilink to a file inside, one bare note); shape exercises both directions of the backlink-update path. Harness asserts fixture structure + windows-preflight.md Step 5 + Step 6 content + 4 SKILL.md cross-refs. Empirical Windows-side verification (broken pattern fails, fixed pattern succeeds) is documented in fixture README — to be run on a Windows host before any change to enumeration in the four launch-scope skills. |
| 2026-05-07 | references/clone-cluster-detection.md | Created — shared utility doc for the W2 mode-shift unification. Two recipes: (a) `is_birthtime_in_clone_cluster_window` reads filesystem birthtime via `stat -f '%SB'` (Darwin) or `stat -c '%W'` (Linux, with mtime fallback) and compares to a vault-scope cluster window; (b) `has_alternate_date_source` walks Source Hierarchy Prio 1-3 (YAML `created`, filename `YYYY-MM-DD`, git first-commit) without falling through to filesystem birthtime. Decision matrix: cluster-AND-no-alt-source → SKIP `created` enrichment; otherwise PROCESS via Source Hierarchy. Cluster-window heuristic: ≥10 files in any 1 h bin → declare cluster (median ±30 min tolerance). Recipe (a) self-guards on unset window vars (returns 1 = PROCESS). |
| 2026-05-07 | property-enrich | Step 3a NEW — clone-cluster gate before Source Hierarchy Prio 4 (filesystem birthtime). Files in cluster window with no alt source SKIP `created` enrichment (`title` and `modified` still enriched), log Class-C finding "clone-cluster birthtime, no alt source". New `clone_cluster_skip` parameter (default `true`) — set to `false` to disable the gate. Codifies GR-3 Cell 1 mode-shift Option B (2026-05-01) into shipped behavior. |
| 2026-05-07 | note-rename | Step 4b updated — Source Hierarchy Prio 1-3 first; clone-cluster gate before falling to Prio 4. Files in cluster with no alt source SKIP `created` enrichment (rename flow still applies, `created` stays absent). New `clone_cluster_skip` parameter (default `true`). Codifies GR-3 Cell 2 mode-shift Option C (2026-05-01). |
| 2026-05-07 | inbox-sort | Step 5b updated — Source Hierarchy Prio 1-3 first; clone-cluster gate before falling to Prio 4. Files in cluster with no alt source SKIP `created` Nahbereich-enrichment (inbox routing still runs, cooldown evaluation uses filesystem birthtime read for cooldown-only purposes). New `clone_cluster_skip` parameter (default `true`). Codifies GR-3 Cell 3 mode-shift Option D (2026-05-01). |
| 2026-05-07 | property-describe | Step 2c NEW — clone-cluster gate for cooldown evaluation. Files in cluster window with no alt source DEFER description generation (cooldown undecidable), log Class-C finding "clone-cluster birthtime, no alt source — cooldown undecidable", report in Skipped section. New `clone_cluster_skip` parameter (default `true`). |
| 2026-05-07 | tests + scripts | New fixture `tests/fixtures/clone-cluster/` (deterministic generator, 30 files / 4 cells, `_truth.json`). New assertion harness `scripts/test-clone-cluster.sh` (6 sections: fixture structure, generator, decision matrix, recipe-doc content, 4 SKILL.md cross-refs, grep-uniqueness). Verifies 1 implementation in references/clone-cluster-detection.md and N callers — no SKILL.md reimplements the birthtime-stat pattern. |
| 2026-05-07 | scripts/detect-clone-cluster.sh | **v0.1.4 W3** — created. Runnable cluster-window detector: enumerates `.md` files under a vault root, reads filesystem birthtime cross-platform (Darwin `stat -f '%B'`, Linux `stat -c '%W'` with `%Y` fallback), bucketizes into 1 h UTC bins, and emits `CLUSTER_FOUND=yes/no` plus window bounds + file count when any bin meets the 10-file floor. Eval-friendly KEY=VALUE output. Single source of truth for the bucketize-and-find-winner step previously specified only as prose in `references/clone-cluster-detection.md` — fills the W2 implementation gap. |
| 2026-05-07 | references/windows-preflight.md | **v0.1.4 W3** — added Step 7 "Clone-Cluster Birthtime Detection" (WARN-flow, not STOP-flow). Calls `scripts/detect-clone-cluster.sh`; on `CLUSTER_FOUND=yes` emits a non-blocking warning showing file count + window + reference to `clone-cluster-detection.md`, then proceeds with the skill. The runtime SKIP-gate (recipes a + b) remains the data-safety mechanism; this preflight is transparency. Updated "What this preflight does NOT check" (dropped the stale "Clone method... is a separate concern" bullet now that Step 7 covers it) and "Cost" (added Step 7 detector pass timing). New "Test fixture" subsection cross-referencing `scripts/test-clone-preflight.sh`. |
| 2026-05-07 | references/clone-cluster-detection.md | **v0.1.4 W3** — Cluster Window Detection section now points at `scripts/detect-clone-cluster.sh` as the runnable implementation. Runtime SKIP-gate (recipes a + b) and preflight WARN (`windows-preflight.md` Step 7) call the same detector. |
| 2026-05-07 | docs/windows-considerations.md | **v0.1.4 W3** — retracted the unconditional "robocopy preserves CreationTime from source" claim. TL;DR §1, Clone-Method Behavior table (row 3), and per-method explanation paragraph now reflect empirical reality: `robocopy /COPY:DAT` preserves CreationTime at clone-time, but post-clone Defender / Search Indexer / Obsidian-cache writes can reset it (GR-3 nexus-clone-robocopy 2026-05-01: 36.8 % of inbox-tree files clustered at clone moment). Added a 2026-05-01 update note under the table linking to `clone-cluster-detection.md` for the runtime mitigation and to `windows-preflight.md` Step 7 for the preflight WARN. |
| 2026-05-07 | docs/cloning-guide.md | **v0.1.4 W3** — Windows clone-method table row 2 (`robocopy /E /COPY:DAT`) and surrounding callout retracted the unconditional "preserves both" / "Yes — preserved from source" claim, now reference the empirical 36.8 % cluster from GR-3 plus the runtime mitigation in `clone-cluster-detection.md`. |
| 2026-05-07 | scripts/test-clone-preflight.sh | **v0.1.4 W3** — new assertion harness (8 sections): detector script presence + perms; detector against W2 cluster fixture (`CLUSTER_FOUND=yes`, count ≥ 10, valid ISO-8601 window); detector against synthetic 5-file no-cluster dir (`CLUSTER_FOUND=no` — under floor); detector against empty dir; `windows-preflight.md` WARN-flow section content checks (mentions detector script, says WARN, references `clone-cluster-detection.md`, says "proceed/continue"); `clone-cluster-detection.md` cross-ref to detector; cross-doc retraction guard (refute the stale "Yes — preserved from source" line in both `windows-considerations.md` and `cloning-guide.md`); changelog `v0.1.4 W3` entry presence. |
| 2026-05-07 | 4 SKILL.md preflight references | **v0.1.4 W3** — extended the parenthetical Pre-flight summary in inbox-sort, property-enrich, note-rename, and property-describe to mention the clone-cluster preflight WARN at Step 7 alongside registry check / trailing-dot detection / Windows-aware enumeration pattern. "End-to-end" was already normatively sufficient; this closes the door on a sloppy reader skipping Step 7. |
| 2026-05-07 | W4 recipe-(f) duplicate-key policy | **v0.1.4 W4** — F7 close: divergent-value collisions ABORT recipe (f), file unchanged, Class-A finding "duplicate-key-divergent-values"; identical-value collisions silent-dedup as Class-D; new sanity-check verdicts `DUPLICATE_KEYS_DIVERGENT_VALUES` + `DUPLICATE_KEYS_IDENTICAL_VALUES`; Pattern 5 detection covers pre-existing plain duplicates universally (not only post-shape-β-normalize); 4 launch-scope SKILL.md routing tables updated; 5-cell fixture + 6-section assertion harness (`tests/fixtures/recipe-f-duplicate-keys/`, `scripts/test-recipe-f-duplicate-keys.sh`). |
| 2026-05-07 | plugin.json + marketplace.json | Version bump 0.1.3 → 0.1.4. Marketplace caches by version field; existing 0.1.3 installs need this bump to receive updates. CLAUDE.md status line bumped in lockstep. |
| 2026-05-07 | **v0.1.5** | Cross-platform clone-preflight extraction + recipe (a) runtime correctness fix on non-UTC macOS (extraction lifts the clone-cluster WARN from Windows-only to every OS; runtime fix replaces ISO-string-with-literal-`Z` compare with numeric epoch compare, closing silent wrong-SKIP verdicts on non-UTC macOS hosts that fell through to filesystem birthtime since v0.1.4 W2 ship 2026-05-07) |
| 2026-05-07 | **NOTE for v0.1.4 macOS non-UTC users** | If you ran `property-enrich` (or another date-derivation skill) on a cloned vault under v0.1.4 on a non-UTC macOS host, recipe (a) was returning wrong SKIP verdicts due to a Darwin-only ISO-string-with-literal-`Z` bug, so the skills fell through to filesystem birthtime and wrote clone-time as `created` for cluster files. Just re-running `property-enrich` after upgrading to v0.1.5 is **not sufficient**: `property-enrich` is additive-only (Step 3 only walks the Source Hierarchy `for each note missing created`), so files with already-populated wrong `created` values are skipped silently. Three-step remediation: (1) run `scripts/detect-clone-cluster.sh` against the vault to identify the cluster window (ISO timestamps + file count); (2) for each file whose `created` value falls inside the cluster window, blank the `created` field (delete the YAML line); (3) re-run `property-enrich`. The v0.1.5 SKIP-gate then fires correctly on the blanked files — those with no alt source surface as Class-C "clone-cluster birthtime, no alt source" findings; those with an alt source (filename `YYYY-MM-DD` pattern or git first-commit) get the correct `created` value via Source Hierarchy Prio 1-3. Linux, Windows, and UTC-macOS hosts were not affected. |
| 2026-05-07 | references/clone-preflight.md | **v0.1.5** — created. Cross-platform clone-cluster preflight extracted from `windows-preflight.md` Step 7. Same WARN-flow semantics, same detector script, same WARN message. Difference: this preflight runs on every OS (the underlying clone-cluster condition is not Windows-specific — `cp -R` on macOS, `cp -a` on Linux, `git clone`, ZIP downloads, and `robocopy` on Windows all collapse filesystem birthtimes onto the clone moment). The previous Windows-only invocation meant macOS and Linux users never saw the WARN even when their vault was a fresh clone. |
| 2026-05-07 | references/windows-preflight.md | **v0.1.5** — Step 7 (Clone-Cluster Birthtime Detection) removed; content moved to `clone-preflight.md`. Steps 1-6 unchanged (registry check, trailing-dot detection, Windows-aware enumeration). "Test fixture" section updated — only the trailing-dot fixture is documented here now; clone-cluster fixture documented in `clone-preflight.md`. "What this preflight does NOT check" section adds a pointer at `clone-preflight.md` for the cross-platform clone-cluster concern. "Cost" section drops the Step 7 detector pass timing. |
| 2026-05-07 | references/clone-cluster-detection.md | **v0.1.5** — runnable-implementation paragraph re-routes the user-facing WARN cross-link from `windows-preflight.md Step 7` to the new `clone-preflight.md` (cross-platform). |
| 2026-05-07 | 4 launch-scope SKILL.md preflight blocks | **v0.1.5** — restructured from a single Windows-gated sentence to a numbered two-step block: Step 1 always runs `clone-preflight.md` (every OS); Step 2 additionally runs `windows-preflight.md` end-to-end on Windows. The invariant "the enumeration pattern in Step 6 of `windows-preflight.md` applies to every subsequent file-listing call" is preserved verbatim within Step 2. inbox-sort, note-rename, property-enrich, property-describe all updated. |
| 2026-05-07 | scripts/test-clone-preflight.sh | **v0.1.5** — extended from 8 to 10 sections. Section 5 reads `clone-preflight.md` (was `windows-preflight.md`). Section 6 asserts `clone-cluster-detection.md` cross-refs the new `clone-preflight.md`. Section 8 asserts both v0.1.4 W3 (historical) and v0.1.5 changelog entries. Section 9 (new) asserts `windows-preflight.md` no longer has a Step 7 heading and now cross-links `clone-preflight.md`. Section 10 (new — the contract test) asserts each of the 4 launch-scope SKILL.md files invokes `clone-preflight.md` unconditionally (cross-platform marker required near the reference) and no longer mentions Step 7. Without this contract test the file-extraction would have been undetectable as a behavior change. |
| 2026-05-07 | references/clone-cluster-detection.md | **v0.1.5 (runtime fix)** — recipe (a) `is_birthtime_in_clone_cluster_window` switched from ISO-string compare to numeric epoch compare. v0.1.4 W2 shipped the recipe with `stat -f '%SB' -t '%Y-%m-%dT%H:%M:%SZ'` on Darwin, which formats local time and slaps a literal `Z` suffix. On non-UTC macOS hosts the resulting "ISO" string was off by the local-UTC offset and disagreed with the genuinely-UTC window emitted by `scripts/detect-clone-cluster.sh` — recipe (a) returned **wrong SKIP verdicts on every macOS user in a non-UTC timezone**, causing date-derivation skills to fall through to filesystem birthtime and write clone-time as `created`. The Linux path was already epoch-based and correct. Empirical demonstration captured in commit message + PR body: cell-a-10 on CEST = local 22:21 / UTC 20:21; buggy compare returned NOT IN CLUSTER; new compare returns IN CLUSTER. Migration note added for callers still exporting only the ISO window vars (must add `*_EPOCH` from the detector). |
| 2026-05-07 | scripts/detect-clone-cluster.sh | **v0.1.5 (runtime fix)** — emits two additional fields when `CLUSTER_FOUND=yes`: `CLONE_CLUSTER_WINDOW_START_EPOCH` and `CLONE_CLUSTER_WINDOW_END_EPOCH` (integer seconds since epoch, UTC). Existing ISO string fields preserved (consumed by the WARN message in `clone-preflight.md`). Recipe (a) consumes the epoch fields; UI consumes the strings. Header comment documents the contract and explicitly explains why the ISO-string compare path is unsafe on non-UTC Darwin. |
| 2026-05-07 | tests/fixtures/clone-cluster/generate.sh | **v0.1.5 (runtime fix)** — `set_btime` rewritten to roundtrip through epoch instead of feeding ISO strings to local-time-interpreting `touch -t` (BSD) or `touch -d` (GNU). Both flavors first parse the UTC ISO timestamp to epoch (`date -ju -f` on BSD, `date -u -d` on GNU), then GNU passes `-d "@$epoch"` directly while BSD formats the local-time-equivalent `YYYYMMDDhhmm.SS` for `touch -t`. Without this roundtrip, the fixture's documented UTC instant is shifted by the local-UTC offset of the host running the generator. Cell A stagger reduced from 3-min × 20 (= 57-min span) to 2-min × 20 (= 38-min span) so all 20 cell-A files fit inside the detector's median-±30-min window once the epoch fix exposes the wider-than-window design from W2. Cell B uses `:47` seconds instead of `:23` so any minute-level overlap with cell A still differs at the epoch level. |
| 2026-05-07 | scripts/test-clone-cluster.sh | **v0.1.5 (runtime fix)** — harness now drives recipe (a) from the actual detector output (`scripts/detect-clone-cluster.sh`) instead of re-deriving the window from `_truth.json`. Re-derivation hid the runtime bug because both the harness's UTC compare-window and recipe (a)'s broken local-time-with-Z BTIME-string were systematically wrong by the same local-UTC offset, so the test passed by coincidence. New lock-in assertion: detector window must enclose `_truth.json`'s `center_utc` epoch — v0.1.4 W2 deliberately omitted this assertion because the broken format-string would have made it fail on non-UTC hosts; v0.1.5 fixes the underlying contracts so the assertion now passes cleanly and any future regression to local-time-with-Z trips it immediately. Inline recipe (a) updated to v0.1.5 epoch contract (numeric `-ge`/`-le` compare, no ISO strings). |
| 2026-05-10 | CLAUDE.md | **v0.1.5 (post-#20 cleanup, PR #21)** — inbox-sort status flipped `beta` → `stable` in the skills table to match README and reality (ROADMAP and the README skill-status table both already showed `stable`; CLAUDE.md was the lone holdout). Per Z1 T11 F1. |
| 2026-05-10 | README.md | **v0.1.5 (post-#20 cleanup, PR #22)** — corrected the "Skills count" row in the "How We Compare" table from `7 (3 stable, 4 in dev)` to `7 (3 stable, 3 beta, 1 deferred)` so the comparison-table cell matches the skills-status table directly above (3 stable + 3 in development + 1 deferred). Per Z1 T11 F2. |
| 2026-05-10 | plugin.json + marketplace.json | Version bump 0.1.4 → 0.1.5. Marketplace caches by version field; existing 0.1.4 installs need this bump to receive updates. CLAUDE.md status line bumped in lockstep. |
| 2026-06-20 | references/tag-semantics.md | **v0.1.6 (tag-manage v1)** — created. Documents the mandatory Step 0 finding: Obsidian matches tags case-insensitively (official Help, verbatim + Tag-Wrangler/forum corroboration). Splits the flagship `#AI`/`#ai`/`#a-i` example into two op classes — cosmetic case-normalize (functional no-op, opt-in default OFF) vs functional merge of distinct spellings (irreversible). Locks 7 advisor-reviewed design decisions (case opt-in, frontmatter-only remove, separator grouping excludes `/`, no nested-rename cascade, reserved-tag exclusion, Unicode case-fold for the German vault). |
| 2026-06-20 | references/tag-convention.md | **v0.1.6** — added a "case is cosmetic, not functional" note cross-linking `tag-semantics.md`; clarifies the PascalCase rules are a display convention (source for the target casing when the user opts into case-normalization), not a functional fix. |
| 2026-06-20 | skills/tag-manage/scripts/tags.js | **v0.1.6** — new deterministic tag engine (pure logic, no fs/clock): `logicalKey` (Unicode case-fold, slash-preserving), `isValidTag` (Obsidian validity: ≥1 non-numeric), single-pass survival tokenizer (`scanBody` — fenced/inline code + wikilink regions skipped; ATX heading marker and URL `#frag` excluded by the whitespace/line-start boundary rule), frontmatter reader/rewriter for reps 1-4 (block/inline-array/scalar/legacy `tag:`), `applyOps` (rename/merge/remove across all 6 reps with in-note dedup), structural `assertSurvival` guard (re-tokenize; every non-tag-token byte must match — the analog of ai-paste-cleanup's fingerprint guard), and audit grouping (`caseVariantGroups`, `separatorVariantGroups` excluding `/`, `buildInventory`, `auditFindings`). |
| 2026-06-20 | skills/tag-manage/scripts/cli.js | **v0.1.6** — fs + CLI shell: `audit`/`plan`/`apply` subcommands, plan-then-write (transform all in memory, guards throw before any write), in-place `fs.writeFileSync` (preserves birthtime — reuses inode), `MassChangeError` (default 50-note ceiling, `--max N` override), exit codes 0/1/2. |
| 2026-06-20 | skills/tag-manage/tests/ | **v0.1.6** — 54 node:test assertions (RED→GREEN): `tags.test.js` (40 — logicalKey/validity, the survival tokenizer incl. positive-throw guard test, representation-matrix rename across all 6 reps, merge dedup, frontmatter-only remove with body residual, idempotency), `audit.test.js` (7 — inventory/orphans/case-vs-separator/numeric-artifact/untagged/reserved), `cli.test.js` (7 — walk filtering, dry-run no-write, in-place apply, vault idempotency, plan-then-write no-partial-write, mass-change throw). CI bridge `scripts/test-tag-manage.sh` (auto-discovered by the existing `test-*.sh` loop). |
| 2026-06-20 | skills/tag-manage/SKILL.md | **v0.1.6** — rewrote the `status: deferred` stub into a `status: beta` two-stage write skill (audit+plan read-only → apply behind the confirm gate). Triggers narrowed to existing-tag audit/cleanup (auto-tagging removed — scope C, later version). Documents survival guarantees, the logical-tag matrix, and known limitations. **Deliberate brief-divergence (documented):** v1 writes NO per-note skill-log callout (ai-paste-cleanup precedent for deterministic rewrite scripts; avoids a large incidental footprint when a bulk op touches hundreds of notes; keeps re-runs provably idempotent). The `#143` callout-dedup concern therefore does not apply to v1 — tracked as a follow-up if per-note skill-log is added. |
| 2026-06-20 | tests/fixtures/tag-manage/ | **v0.1.6** — synthetic chaos vault for UAT + CI: notes covering all 6 representations, case variants, separator variants, nested tags, an orphan, a numeric artifact, and the four survival cases (code/URL/heading/wikilink), plus a README documenting the expected audit findings. |
| 2026-06-20 | plugin.json + marketplace.json | **v0.1.6** — version bump 0.1.5 → 0.1.6 for the new tag-manage beta skill. Marketplace caches by version field; existing installs need this bump to receive the skill. CLAUDE.md status line + skills table (tag-manage deferred → beta) bumped in lockstep. |
| 2026-06-20 | **v0.1.7** | tag-manage v2 Slice 1 — convention-compliance engine, curated override dictionaries, rich vault-written report, after-changes report, Dataview `tags::` mis-parse hotfix |
| 2026-06-20 | skills/tag-manage/scripts/convention.js | **v0.1.7** — severity-classified convention checker (`classifyTag`): HIGH (hashtag-prefix, yaml-artifact, numeric-artifact), MEDIUM (snake_case, camelCase, lowercase-concept, upper-kebab), LOW (flat-where-hierarchical); brand-set short-circuit; first-match-wins. `canonicalForm` resolver (brand → compound → PascalCase heuristic); `pascalHeuristic` with `AI-`/`KI-` prefix awareness. |
| 2026-06-20 | skills/tag-manage/references/tag-overrides.default.json | **v0.1.7** — shipped generic defaults: 19 brand entries (GitHub, ChatGPT, YouTube, LinkedIn, n8n, SaaS, SEO, GPT, LLM, MQTT, ETF, CMS, API, docker, Figma, Telegram, Instagram, NotebookLM) and 19 compound entries (OpenSource, LowCode, DayTrading, DeepResearch, ClaudeCode, SystemPrompt, KnowledgeManagement, CodeReview, CodeQuality, GenerativeAI, SoftwareDevelopment, AppDevelopment, WebScraping, SelfHosted, UXDesign, AI-ML, AI-Agents, AI-Coding). MIT-licensed; no vault-specific personal entries. |
| 2026-06-20 | skills/tag-manage/scripts/config.js | **v0.1.7** — `extractJsonFence` (reads first `json` code fence from a Markdown note), `mergeOverrides` (defaults + vault-local → merged dict with `brands`/`compounds` Maps, `brandHyphenSet`, `folderExclusive`, `reportDir`; vault-local wins on collision), `loadConfig` (reads defaults JSON + optional config note text). |
| 2026-06-20 | skills/tag-manage/scripts/analysis.js | **v0.1.7** — frequency/coverage/depth aggregation: total/tagged/untagged notes, unique tags, total assignments, avg tags/note, max depth, top-20 tags, depth distribution, singletons, low-usage tags. |
| 2026-06-20 | skills/tag-manage/scripts/recommend.js | **v0.1.7** — `buildRecommendations`: priority-sorted `[{id, kind, severity, from, to, notesAffected, source, ops}]`. Dictionary-backed canonicals enforced unconditionally (uniform-lowercase brand → official casing); heuristic canonicals proposed only on a real violation (protects compliant tags from wrong heuristic guesses). `kind` = `rename` (one→one) or `merge` (N→one). |
| 2026-06-20 | skills/tag-manage/scripts/report.js | **v0.1.7** — deterministic Markdown report builder: YAML frontmatter header, summary callout, Key Metrics table, Top 20 Tags table, Recommendations table (id, action, severity, from, to, notes affected, source; heuristic entries flagged "verify casing (not in dictionary)"), Next Steps callout, Health Score table, Update Log table. After-changes report on `apply --write` when report directory is configured (suffix: ` - after changes`). |
| 2026-06-20 | skills/tag-manage/scripts/cli.js | **v0.1.7** — extended CLI: `audit <vault> [--report-dir DIR] [--config FILE] [--date YYYY-MM-DD]` (runs full analysis pipeline, writes report + `.tag-manage-recommendations.json` to reportDir if configured); `plan`/`apply <vault> (--ops FILE | --from-recs FILE [--ids 1,3]) [--max N] [--write]` (apply from recommendation IDs or hand-crafted ops); after-changes re-audit on successful `--write`. Config discovery: auto-finds `Tag Manage Config.md` by filename; `--config` overrides. Report directory excluded from audit scan (no self-poisoning). |
| 2026-06-20 | fix(tag-manage) | **v0.1.7** — Dataview `tags::` hotfix: `FIELD_RE` negative lookahead `(?!:)` prevents `tags::` lines from being parsed as YAML tag fields. `tags::` lines are now correctly ignored; Dataview tags remain outside the inventory (not yet first-class). |
| 2026-06-20 | skills/tag-manage/SKILL.md | **v0.1.7** — rewritten for v2 two-stage flow. Documents the compliance engine + severity table, canonical resolver (brand→compound→heuristic, heuristic flagged "verify casing"), override dictionaries (shipped defaults + vault-local config note, config discovery, first-run seeding), report destination (reportDir config or --report-dir, stdout fallback), after-changes report, the unchanged survival + mass-change guarantees, Production Vault Safety Rules note, and Dataview limitation. |
| 2026-06-20 | references/tag-convention.md | **v0.1.7** — added "Override dictionaries" section pointing to `skills/tag-manage/references/tag-overrides.default.json` (shipped generic defaults) and vault-local `Tag Manage Config.md` as the canonical override store for the tag-manage compliance engine. |
| 2026-06-20 | plugin.json + marketplace.json | **v0.1.7** — version bump 0.1.6 → 0.1.7 for tag-manage v2 Slice 1 (compliance engine, dictionaries, rich report). Marketplace caches by version field; existing installs need this bump to receive the update. CLAUDE.md skills table updated in lockstep. |
| 2026-06-20 | fix(tag-manage) recommend.js | **v0.1.7 (UAT pass on production-copy, 1290 notes)** — three real-data fixes. (b) **Acronym preference:** when the canonical resolver falls back to the Title-case heuristic but the vault already uses a real all-caps spelling, that acronym wins (`geo`+`GEO`→`GEO`, not `Geo`; `B2B`, `PRD` likewise) — `isAcronym` = all-caps letters/digits, ≥2 chars, ≥1 letter; new `source: 'acronym'`. (c) **Invalid tags are report-only:** invalid/numeric artifacts (`1`, `1-3`, `Make.com`) no longer produce a rename rec (the `1-3 → 13` bug); they stay in the "Invalid tags — review" section, never in "apply all" (do-no-harm: no silent destructive removal by default). Meaningful alphanumerics (`5G`, `4G`, `9a`) are valid and untouched. (d) **Duplicate folding:** a real case/spelling duplicate of one logical tag now folds even with no classifyTag violation (`AI-Testing`/`AI-testing`), while a single compliant non-dict tag stays untouched (do-no-harm PIN test). |
| 2026-06-20 | fix(tag-manage) tag-overrides.default.json | **v0.1.7 (UAT)** — fixed the `"mcp": "mcp"` bug (acronym was being lowercased across notes) → `"MCP"`. Seeded generic tech acronyms/brands so single-casing acronyms resolve without a co-existing all-caps variant: CLI, DNS, E2E, KPI, PRD, SDK, SQL, UI, UX (brands); DevOps, FinTech, NextJS (compounds). MIT-safe generic entries only; A→Z sorted. |
| 2026-06-20 | feat(tag-manage) report.js | **v0.1.7 (UAT)** — thousand separators on integer counts (`1272` → `1,272`) across summary, Key Metrics, Top-20, and singleton/low-usage counts. Deterministic (`fmt` via regex, not `toLocaleString`); non-integers pass through. |
| 2026-06-21 | feat(tag-manage) | tag-manage: first-run report-home gate — `suggest-report-dir` proposes a smart fresh location based on vault structure, `set-report-dir` persists it to `Tag Manage Config.md` as the permanent home; follow-up reports auto-land there. |
| 2026-06-21 | fix(tag-manage) | tag-manage fix: `audit` creates the report dir (`mkdir -p`) instead of aborting on ENOENT. |
| 2026-06-21 | fix(tag-manage) | tag-manage fix: `apply` excludes report artifacts inside the report dir — a report note can no longer be rewritten ("self-poisoned") by an apply run. |
| 2026-06-21 | **v0.2.0** | tag-manage v2 milestone + first-run report home — compliance engine, override dictionaries, acronym-preference resolver, report-only invalid tags, case-variant folding (validated on a 1,290-note production copy); plus the first-run report-home gate (detect → propose → persist into `Tag Manage Config.md`) and its two coupled fixes (`mkdir -p`, apply self-poisoning). 127 node:test assertions green. |
| 2026-06-21 | plugin.json + marketplace.json | **v0.2.0** — version bump 0.1.7 → 0.2.0. The v2 compliance work (#43) and its UAT fixes (#44) both shipped under 0.1.7 without a bump, then report-home (#45) too — three feature sets shared one version, so existing installs got no marketplace update signal (marketplace caches by version field). This bump restores an honest version. CLAUDE.md status line + ROADMAP top section updated in lockstep. |
| 2026-06-21 | fix(tag-manage) report.js | **v0.2.1** — report self-poisoning fix (OBI-2026-06-21-2). The Next Steps callout emitted bare `#1, #3` / `#2` example tokens; obsidian-linter (move-tags-to-yaml) promoted them into the report's own frontmatter as integer tags, recurring on every save. Removed the `#` (now "apply 1, 3" / "skip 2"); shared `REPORT_MARKER_TAG` constant introduced. Regression test asserts no `#[\w/-]` token in any report output. Found by live UAT on a throwaway production copy. |
| 2026-06-21 | fix(tag-manage) cli.js | **v0.2.1** — report-artifact exclusion broadened to a frontmatter marker (OBI-2026-06-21-3 / F1). Filename-only exclusion missed sibling artifacts (Master Summary, Tag Index, Cookbook, Roadmap, overview notes) that carry `Meta/TagManagement` but not the dated `Tag Analysis Report - *.md` name — they were scanned (inventory pollution) and apply-eligible (rewritten on `--write`). Now excluded by marker too, gated to a non-root report dir so reportDir == vault root still scans every real note. Shared `excludeReportArtifacts` helper across the audit + apply paths. |
| 2026-06-21 | **v0.2.1** | tag-manage report-home trust fixes — the two release-blocking bugs the 2026-06-21 live UAT surfaced (report frontmatter self-poisoning via obsidian-linter + sibling-artifact exclusion). 131 node:test assertions green; full repo CI loop green. Both corrupt data silently in the presence of obsidian-linter, which the target audience commonly runs. |
| 2026-06-22 | feat(tag-manage) hierarchy.js + config.js + cli.js + report-home.js | **Hierarchy nest mechanics (Phase 1, beta — no version bump yet).** Deterministic declared-hierarchy nesting: a flat tag that is a declared child is promoted to `Parent/Child` (e.g. `daytrading` -> `Investing/DayTrading`). New `scripts/hierarchy.js`: `parseHierarchy` (parent->children -> childKey->{parent,child,path} map + hard validation: invalid tag / one-parent-per-child / no cycles; invalid entries reported + excluded), `buildNestRecommendations` (a separate `nest` rec class, opt-in per id, NOT bundled into default cleanup; rides the existing applyOps/survival/mass-change write path — NEST = a rename onto a slash path, zero new write code; converges), `upsertHierarchyCluster` (validated additive config merge, REFUSES an invalid cluster on write). config.js `mergeOverrides` carries the vault-local `hierarchy` block through. cli.js `runAudit` emits nest recs to a SEPARATE `.tag-manage-nest.json` (applied via the existing `--from-recs`/`--ids` path) + the `audit` command surfaces them and reports hierarchy config errors. New `set-hierarchy <vault> --parent --children` CLI writer (deterministic, no LLM). Shared `updateConfigNote` helper extracted from `setReportDir` (single-sourced fence/create/preserve). +29 node:test assertions (160 total), zero regression. The AI-driven content-inferred version is Phase 2 (`tag-organize`, a separate future skill). Design: `docs/superpowers/specs/2026-06-22-tag-manage-hierarchy-design.md`. |
| 2026-06-22 | fix(tag-manage) cli.js | **Nest sidecar convergence fix (F-NEST-1, found by live UAT on a 1,638-note Nexus clone).** After a nest was applied, a re-audit computes 0 nest recs (convergence), but `.tag-manage-nest.json` was written only when recs > 0 — so the stale recs stayed on disk while the report `.md` and `.tag-manage-recommendations.json` correctly showed none. Result: a divergence between the human-facing report ("no nests") and the machine sidecar ("4 nests"), violating the skill's "report is the source of truth" contract. Non-destructive (re-applying the stale file is a no-op — the flat children no longer exist), but a trust defect. Fix: write the nest sidecar when recs are present OR a sidecar already exists, so a converged re-audit clears it to `[]` (mirrors the unconditional `.tag-manage-recommendations.json` write); a vault that never had a hierarchy still gets no sidecar. The prior convergence test only asserted the return value was `[]`, never the on-disk artifact — the new test asserts the file. +1 node:test assertion (164 total), full CI loop green. |
| 2026-06-23 | feat(tag-organize) induce.js + cli.js + skills/tag-organize/SKILL.md | **tag-organize Slice 1 (induce-structure, beta — no version bump yet).** New skill `tag-organize` (Phase 2 split from tag-manage; shares the engine). New `scripts/induce.js` (PURE: `tokenizeTag` / `leadingSegment` split on camelCase / `-` / `_` / `/` / letter-digit boundaries; `clusterByName` groups flat residual tags sharing a leading token into `{parent, children, basis}` candidate families — >= minMembers distinct, excludes reserved + already-nested, children A->Z case-insensitive, parent = most-frequent leading display segment). cli.js `runInduce` + `induce <vault>` command builds the inventory, runs `clusterByName`, writes `.tag-organize-clusters.json` (dot-prefixed -> never scanned by walkMarkdown, no self-poisoning). SKILL.md orchestrates hygiene-first -> induce -> review table -> content-read (gated) for uncertain families -> set-hierarchy per approved cluster -> apply nests via the existing guarded path (no new write code in Slice 1 — a nest rides applyOps). Slice 1 = structure over existing tags only; auto-tag (`addTagsToNote`, content-read of under-tagged notes, closed+gated-new vocab, approval-at-scale) is Slice 2, not built. +5 node:test assertions (169 total), full tag-manage suite green. Design: `docs/superpowers/specs/2026-06-23-tag-organize-design.md`; plan: `docs/superpowers/plans/2026-06-23-tag-organize-slice-1-induce.md`. |
| 2026-06-24 | feat(tag-organize) report.js + cli.js + skills/tag-organize/SKILL.md | **Slice 1.5 — human-readable induce proposal + report-filename stamp regression fix (OBI-2026-06-24-2 findings 1+2).** Finding A: `resolveReportContext` lost the `HHMM` filename stamp (`slice(0,10)` truncation) so same-day audit re-runs overwrote each other; new pure `reportStamp(iso, hasExplicitDate)` (`''` with `--date`, else UTC `HHMM`) threaded through `runAudit` + induce. Finding B: new pure `renderProposal` in report.js (marker frontmatter, backtick-wrapped families, no bare `#token` — the OBI-2026-06-21-2 linter-promotion invariant); `runInduce` now excludes report artifacts before scanning and writes a browsable `Tag Organize Proposal - Vault-wide.md` note when a report home is configured (mirrors the audit; dot-sidecar still always written); `isReportArtifact` regex generalized to `Tag (Analysis Report|Organize Proposal)` (covers the root case where the marker path is gated off). TDD caught a real mkdir-order bug (sidecar write preceded report-home creation). +9 node:test assertions (173 -> 182), full tag-manage suite green. Captured finding: a configured `reportDir` makes "audit"/"induce" WRITE even without `--report-dir` (documented in SKILL.md). Design: `docs/superpowers/specs/2026-06-24-tag-organize-slice-1.5-design.md`; plan: `docs/superpowers/plans/2026-06-24-tag-organize-slice-1.5.md`. |
